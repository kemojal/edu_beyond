export const DEFAULT_PORT = 3003;
export const MAX_FILE_SIZE_MB = 10;
export const KB_IN_BYTES = 1024;
export const MB_IN_BYTES = KB_IN_BYTES * 1024;
export const MAX_FILE_SIZE_BYTES = MAX_FILE_SIZE_MB * MB_IN_BYTES;

export const FILE_TYPES = ["application/pdf"];
export const UPLOAD_PATH = "uploads";

export const MAX_REQUEST = 10;
export const WINDOW_SIZE_MS = 60 * 1000;
